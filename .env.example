# Used by the server (SSR loaders). Must include /api.
API_URL=http://127.0.0.1:8000/api

# Used for production browser builds. In local dev the app uses the Vite proxy (/api).
VITE_API_BASE_URL=http://127.0.0.1:8000/api
